<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="greeting">
      <h1>HEY</h1>
      <h2>Welcome to the site!</h2>
    </div>
    <h3>the messaging site</h3>
    <h4>make an account, login, post a message</h4>
   
    <hr>
    <div class="links">
      <a href="/createUser"><button>Create an account</button></a> <b>|</b> 
      <a href="/login"><button>Login</button></a> <b>|</b> 
      <a href="/message"><button>post a message</button></a> <b>|</b> 
    </div>
    <hr>

    <h2>Messages</h2>
    <div class="messageCont">
    <% for (let message of data) { %> 
       <% let username = message.dataValues.User.dataValues.username %>
       <% let msg = message.dataValues %>
        <div class="messages">
          <b><%= username %></b> <span class="someText">posted: <%= msg.content %></span>
          <span class="tick">
              <form action="/score" method="post">
                <input type="text" id="content" name="content" value="<%= msg.id %>">
                <input type="submit" id="tick" value="&#9745">
            </form>
        </span>
        <% for(let like of likees) { %>
            <div class="likes">Likes: <%= like.dataValues.likes %></div>
        <% } %> 
        </div>
    <% } %>
    </div>    
</body>
</html>